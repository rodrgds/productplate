# Blog content

Add published posts as `.svx` files. Every post needs `title`, `description`, `author`, `published`, optional `updated`, `tags`, `draft`, `canonicalUrl`, and `socialImage` frontmatter.
