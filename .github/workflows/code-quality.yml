name: Code Quality Checks

on: [push, pull_request]

env:
  # Required environment variables for type checking
  # PUBLIC_* vars - required for client code
  PUBLIC_CONVEX_URL: 'https://fake-convex-url.convex.cloud'
  PUBLIC_CONVEX_SITE_URL: 'https://fake-convex-site-url.convex.site'
  # SITE_URL - imported in hooks.server.ts and admin/+layout.server.ts
  SITE_URL: 'https://fake-site-url.com'
  # Better Auth rejects its package default during production-mode builds.
  # This CI-only value is never deployed or used by a real environment.
  BETTER_AUTH_SECRET: 'ci-build-only-secret-not-used-at-runtime'

jobs:
  lint_and_test:
    name: Lint, Typecheck, and Test
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.3

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Install browser
        run: bunx playwright install --with-deps chromium

      - name: Lint
        run: bun run lint

      - name: Typecheck
        run: bun run check

      - name: Unit tests
        run: bun run test:unit

      - name: Build
        run: bun run build

      - name: Browser smoke tests
        run: bun run test:e2e:ci
