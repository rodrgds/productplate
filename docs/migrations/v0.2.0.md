# Product Plate v0.2 migration

Direct full-demo clones remain supported for inspection, but new founder apps should be regenerated with `bun create product-plate` and one fixed profile. Copy product code into the generated app rather than deleting showcases by hand.
